{
  "name": "@ezview/eslint-config",
  "type": "module",
  "version": "1.0.0",
  "description": "基于 @antfu/eslint-config 封装的相对宽松的 eslint-config",
  "main": "src/index.js",
  "files": ["README.md", "src"],
  "scripts": {
    "publish:only": "pnpm publish --access public --no-git-checks --registry=https://registry.npmjs.org"
  },
  "peerDependencies": {
    "eslint": "^9"
  },
  "dependencies": {
    "@antfu/eslint-config": "^5.1.0"
  }
}
